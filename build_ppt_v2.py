"""
Build 10-page PPT structure — 以同学两传感器方案为主线
每页详细解释: 图在讲什么、PPT要传达什么信息、具体如何布局
"""
import sys; sys.stdout.reconfigure(encoding='utf-8')
import os, shutil
from pathlib import Path

BASE = Path('d:/数理统计大作业数据')
S2 = BASE / 'GongLi-buaa-2-main'  # 主线: 同学两传感器
US = BASE  # 对比: 我们四传感器

PPT = BASE / 'ppt_slides_v2'
if PPT.exists(): shutil.rmtree(PPT)
PPT.mkdir()

# ============================================================
# Page 01: 研究背景与动机
# ============================================================
d = PPT / 'page_01'; d.mkdir()
shutil.copy(S2 / 'outputs/figures/s2_main_damage_histogram.png', d / '01_s2_histogram.png')
with open(d/'说明.txt', 'w', encoding='utf-8') as f:
    f.write("""=== 这一页讲什么 ===
提出核心问题: 卫星遥感可以大面积监测冲突损害, 但单一传感器只能看到"损毁"的一个侧面。

Sentinel-2(光学卫星): 白天拍照, 检测地表颜色变化——建筑炸塌了会改变地表光谱, 植被烧毁了NDVI会下降。但受云层和季节影响。

VIIRS(夜间灯光卫星): 晚上拍照, 检测城市灯光亮度——断电/撤离→灯光变暗。但无法区分"建筑炸了"和"停电了"。

UNOSAT: 联合国专家在高分辨率卫星图上肉眼标注哪些建筑被毁——最可靠但有主观性, 且标注量有限。

本研究的核心问题: 如果两个传感器对同一个地方给出了不同的"是否损毁"的判断, 如何用统计方法把它们整合起来, 同时诚实量化"我们有多不确定"?

=== 图片说明 ===
图是S2损毁评分的直方图分布。横轴是损毁评分(越正=越像损毁), 纵轴是像素数量。图中大部分像素接近0(没什么变化), 少量像素偏离(可能被损毁)。这幅图告诉你: "损毁"信号在数据中是一个长尾分布, 不是黑白分明的。

=== 制作建议 ===
- 标题(28pt): "一个问题, 两张不同的答案"
- 图放中间偏右, 占页面60%
- 左侧放三行文字(18pt):
  "Sentinel-2: 测光谱 → 结构变化"
  "VIIRS: 测灯光 → 功能丧失"
  "UNOSAT: 人眼标注 → 外部参考"
- 底部一句话总结: "三个信息源看到的是损毁的不同维度"
""")

# ============================================================
# Page 02: 我们用了什么数据
# ============================================================
d = PPT / 'page_02'; d.mkdir()
shutil.copy(S2 / 'outputs/figures/viirs_ntl_pre_post.png', d / '02a_viirs_pre_post.png')
shutil.copy(S2 / 'outputs/figures/s2_main_damage_score.png', d / '02b_s2_damage_score.png')
with open(d/'说明.txt', 'w', encoding='utf-8') as f:
    f.write("""=== 这一页讲什么 ===
展示两种卫星数据具体长什么样, 让大家直观感受到"S2和VIIRS看到的是完全不同的东西"。

=== 左图说明 (02a) ===
VIIRS夜间灯光图, 两张对比: 战前(2021年5月) vs 战后(2022年5月)。
图上每个像素 = 500m×500m方块。颜色越亮 = 灯光越强(城市核心、工厂、居民区)。
战前图: 马里乌波尔市区有明显的高亮区(市中心)。战后图: 市区灯光大面积变暗甚至消失。
这幅图传达: "战争让一座城市从亮变暗"——这是功能性的损害(断电、撤离)。

=== 右图说明 (02b) ===
Sentinel-2多光谱损毁评分图。这是对May→May的三项光谱指数(NDVI植被、NBR火烧、NDBI建筑)做PCA降维后, 计算的方向性损毁评分。
颜色越红 = 损毁评分越高(地表光谱发生了"破坏方向"的显著变化)。
这幅图传达: S2看到的损毁和VIIRS看到的损毁, 空间分布完全不同——VIIRS说"市中心灯灭了", S2说"市郊有些地方光谱变了"。

=== 关键对比 ===
两幅图放在一起, 一眼就能看出: S2和VIIRS在"哪些地方被损毁了"这个问题上几乎没有共识。这不是错误, 它们测的是不同物理量。

=== 制作建议 ===
- 左图上方标注"VIIRS夜间灯光 (500m)", 右图上方标注"Sentinel-2损毁评分 (10m→500m)"
- 两张图等大左右并排
- 底部一行文字(16pt): "同一城市, 同一时期, 两个传感器看到完全不同的'损毁'模式"
""")

# ============================================================
# Page 03: Sentinel-2如何从卫星影像变成损毁评分
# ============================================================
d = PPT / 'page_03'; d.mkdir()
shutil.copy(S2 / 'outputs/figures/s2_main_delta_indices.png', d / '03_s2_indices.png')
with open(d/'说明.txt', 'w', encoding='utf-8') as f:
    f.write("""=== 这一页讲什么 ===
详细拆解Sentinel-2的数据处理流程——从原始卫星影像到最终的"损毁评分", 每一步在做什么。

=== 处理流程(共5步) ===

第1步: 读取波段
 Sentinel-2卫星每5天飞过一次马里乌波尔, 每次拍摄13个"颜色通道"(波段)。
 我们需要的是: B04(红光)、B08(近红外)、B11(短波红外1)、B12(短波红外2)。
 战前: 2021年5月23日。战后: 2022年5月8日。为什么选5月对5月?
 → 因为1月和5月的植被差异巨大(冬天枯黄 vs 春天翠绿), 如果用1月对5月, 变化信号90%是季节效应, 不是战争损害。

第2步: 计算三个遥感指数
 NDVI = (B08-B04)/(B08+B04) → 植被量。越绿越高。
 NBR  = (B08-B12)/(B08+B12) → 火烧/地表变化。烧过的地方NBR会骤降。
 NDBI = (B11-B08)/(B11+B08) → 建筑/裸土指数。楼塌了NDBI会变。

第3步: 计算变化量 Δ = 战后 - 战前
 ΔNDVI: 负值=植被减少了(可能是被炸/烧了)
 ΔNBR:  负值=地表发生了火烧/变化
 ΔNDBI: 正值=裸土/废墟增加

第4步: PCA降维
 三个Δ指数高度相关(植被减少的同时地表往往也被烧了)。
 PCA找到一个"综合变化方向"——第一主成分(PC1)解释98.3%的方差。
 PC1↑ = NDVI↑+NBR↑+NDBI↓ → 植被变多(非损毁方向)
 PC1↓ = NDVI↓+NBR↓+NDBI↑ → 破坏方向
 我们只取max(0, -PC1)作为损毁评分——只在破坏方向上计分。

第5步: GMM分离"正常变化"和"异常变化"
 损毁评分接近0的像素→正常年际波动。评分很大的像素→可能是损毁。
 GMM自动找出一个阈值, 给每个像素分配一个"属于损毁类"的概率。

=== 图片说明 ===
03图是三个Δ指数在空间上的分布图。左=ΔNDVI(植被变化), 中=ΔNBR(火烧变化), 右=ΔNDBI(建筑变化)。
可以看到: 三个指数变化最剧烈的区域不完全一致(因为每个指数对不同类型的损毁敏感度不同), 这正是需要PCA融合的原因。

=== 制作建议 ===
- 标题: "Sentinel-2数据处理: 从13个波段的卫星影像到1个损毁评分"
- 左侧放流程图(5步, 每步一行文字+箭头), 占35%
- 右侧放三面板Δ指数图, 占65%
- 底部一个关键数字高亮: "PC1解释98.3%方差, 方向感知后43.3%像素被分类为损毁方向变化"
""")

# ============================================================
# Page 04: VIIRS如何从灯光数据变成损毁评分
# ============================================================
d = PPT / 'page_04'; d.mkdir()
shutil.copy(S2 / 'outputs/figures/viirs_ntl_loss_rate.png', d / '04a_viirs_loss.png')
shutil.copy(S2 / 'outputs/figures/p_viirs_damage_probability.png', d / '04b_viirs_prob.png')
with open(d/'说明.txt', 'w', encoding='utf-8') as f:
    f.write("""=== 这一页讲什么 ===
展示VIIRS夜间灯光数据的处理流程和结果。

=== 处理流程 ===

第1步: 读取VNP46A3月度合成产品
 我们用的是月合成产品(VNP46A3), 比日产品(VNP46A2)更稳定——一个月内取高质量像素的中位数,
 避免了单日产品中云、月光、杂散光等噪声。
 战前: 2021年5月。战后: 2022年5月。

第2步: 质量筛选
 只保留Mandatory Quality Flag ≤ 1的高质量像素(排除云覆盖、月光污染、杂散光)。

第3步: 计算NTL损失率
 NTL_loss = 1 - NTL_post/NTL_pre (战前灯光>0.5的像素)
 损失率∈[0,1]: 0=灯光没变, 1=灯光完全消失
 这个归一化很重要——城市核心区战前灯光上千, 乡村只有个位数, 不做归一化的话城市变化会淹没乡村。

第4步: 用Beta分布拟合
 把NTL损失率拟合到一个Beta分布中, 取其CDF值作为损毁概率。
 这比GMM更平滑——不会强制分成两堆, 而是给出一个连续的"相对极端程度"。

=== 左图说明 ===
VIIRS战前/战后灯光损失率的空间分布图。深色=灯光大幅下降(>80%), 浅色=变化不大。
可以看到: 马里乌波尔市区核心(图中的高损毁集中区)灯光损失率达80-100%, 而周边郊区损失率较低。

=== 右图说明 ===
VIIRS损毁概率图(p_viirs)。这是左图的"概率化"版本——把损失率映射到[0,1]的损毁概率。
注意: 大部分像素的p≈0(没什么变化), 只有少数像素p>0.5(很可能损毁了)。

=== 制作建议 ===
- 标题: "VIIRS夜间灯光: 从辐射亮度到损毁概率"
- 左图上方标"NTL损失率 (0=没变, 1=全灭)", 右图上方标"损毁概率 p_viirs"
- 两张图等大左右并排
- 底部标注关键数字: "市区核心NTL下降80-100%"
""")

# ============================================================
# Page 05: 贝叶斯层次融合模型 — 核心方法
# ============================================================
d = PPT / 'page_05'; d.mkdir()
shutil.copy(S2 / 'paper/figures/fig6_conceptual_model.png', d / '05_conceptual_model.png')
with open(d/'说明.txt', 'w', encoding='utf-8') as f:
    f.write("""=== 这一页讲什么 ===
这是整个研究最核心的方法。讲清楚"贝叶斯层次融合模型"是什么, 它如何把两个传感器的信息整合起来。

=== 问题的本质 ===
每个像素i, 我们有两个传感器的输出:
  p_s2[i]: Sentinel-2说"这个像素有X%概率被损毁了"
  p_viirs[i]: VIIRS说"这个像素有Y%概率被损毁了"
  X和Y常常不一样——因为两个传感器测的是不同物理量。

我们需要回答: "给定S2说X, VIIRS说Y, 这个像素真实被损毁的概率是多少?"

=== 贝叶斯层次模型(三层结构) ===

引入一个隐变量 z_i = 像素i的"真实损毁概率"(我们永远观测不到它, 但可以用统计推断估计它)。

Level 3 (全局先验):
  所有像素的真实损毁概率 z_i 来自一个Beta(α,β)分布。
  α和β是超参数——它们描述了"研究区域内, 损毁概率的整体分布是什么形状"。
  初始时我们设α=β=1 → Beta(1,1)=均匀分布 → "在拿到任何数据之前, 我对损毁概率一无所知"。

Level 2 (传感器观测):
  传感器k的输出 p_ik 是对z_i的有噪声观测:
  p_ik ~ Beta(κ_k * z_i, κ_k * (1-z_i))
  κ_k是"传感器精度"——κ越大, 该传感器的输出越可靠。
  如果κ_viirs > κ_s2 → VIIRS在融合中话语权更大。

Level 1 (后验推断):
  用MCMC(NUTS采样器, 4条链×2000次迭代)推断每个z_i的后验分布。
  输出: z_i的后验均值(损毁概率) + 95%HDI(不确定区间)。

=== 为什么用MCMC而不是解析解? ===
因为κ_k本身也是需要估计的参数。解析Beta-Binomial共轭只在κ_k已知时成立。
我们要同时推断z_i和κ_k → 必须用MCMC。

=== 图片说明 ===
概念模型图展示了三层贝叶斯结构的图形化表示:
- 顶部: 全局超参数α, β
- 中层: 传感器的隐变量z_i
- 底层: 观测数据(p_s2, p_viirs)
箭头表示条件依赖关系。这个图也叫"概率图模型"。

=== 制作建议 ===
- 标题: "贝叶斯层次融合: 给定两个传感器说不同的话, '真实'损毁概率是多少?"
- 左侧: 概念模型图(概率图), 占55%
- 右侧: 三层结构的文字说明, 每层2-3行, 占45%
- 底部加一个公式框: z_i ~ Beta(α,β) → p_ik|z_i ~ Beta(κ·z_i, κ·(1-z_i))
""")

# ============================================================
# Page 06: 融合结果 — 核心发现
# ============================================================
d = PPT / 'page_06'; d.mkdir()
shutil.copy(S2 / 'paper/figures/fig2_sensor_disagreement_v4.png', d / '06_disagreement.png')
with open(d/'说明.txt', 'w', encoding='utf-8') as f:
    f.write("""=== 这一页讲什么 ===
展示融合的最核心发现: 两个传感器几乎完全不相关, 融合结果几乎退化为均匀分布。

=== 左图详解 (传感器分歧图) ===
这是一个四面板图, 每一面板的含义:
  (a) p_s2 空间分布: Sentinel-2的损毁概率。可以看到有空间纹理——有些区域高(>0.6), 有些区域低(<0.2)。
  (b) p_viirs 空间分布: VIIRS的损毁概率。几乎全图都接近0(黑色), 只有市中心几个像素高。
  (c) 两个传感器的散点图: 横轴=p_s2, 纵轴=p_viirs。如果两个传感器一致, 散点应该沿着对角线。
      实际上散点几乎是一个"反L形"——S2的值在0-1之间均匀散布, 而VIIRS的值几乎全部在0附近。
      相关性ρ≈0或负值——两个传感器在"哪些像素被损毁"上完全没有共识。
  (d) 传感器差异的空间分布: |p_s2 - p_viirs| 的空间分布。红=分歧大, 蓝=一致。
      可以看到: 几乎整个研究区域都是红色——两个传感器在所有地方都不一致。

=== 核心发现 ===
两个传感器测的是不同的物理维度:
  S2: 测地表光谱变化 → "地表变了吗?"
  VIIRS: 测夜间灯光变化 → "还亮着灯吗?"
这两个问题对应的物理过程完全不同。一栋楼被炸了但电网还在运行→S2看到变化, VIIRS看不到。

=== 关键数字 ===
"融合信息抑制率46.8%": 融合后的后验分布比任何一个单传感器更接近均匀分布(即"完全不确定")。
意思是: 当你把两个互相矛盾的传感器信息放进贝叶斯模型, 模型诚实地告诉你"我不确定"——后验退化了46.8%。
这不是失败——这恰恰证明了"诚实的不确定"比"假装知道"更有价值。

=== 制作建议 ===
- 标题: "核心发现: 两个传感器几乎完全不一致, 融合退化为'我不知道'"
- 左图(传感器分歧图)放中间, 占75%
- 图下方放三个数字大字(24pt): "ρ≈-0.003 | 46.8%信息抑制 | p_fused≈0.155"
- 底部一句话: "传感器分歧不是Bug, 是它们测量的是损毁的不同维度"
""")

# ============================================================
# Page 07: 信息论分析
# ============================================================
d = PPT / 'page_07'; d.mkdir()
shutil.copy(S2 / 'outputs/figures/kld_summary_barplot.png', d / '07a_kld_barplot.png')
shutil.copy(S2 / 'outputs/figures/kld_fusion_suppression_map.png', d / '07b_suppression_map.png')
with open(d/'说明.txt', 'w', encoding='utf-8') as f:
    f.write("""=== 这一页讲什么 ===
用信息论(KLD/JSD)量化"每个传感器到底提供了多少信息"以及"融合后信息被压缩了多少"。

=== 左图说明 (KLD柱状图) ===
KLD = Kullback-Leibler Divergence = "后验分布离先验有多远"
  KLD越大 → 该传感器提供了越多的信息 → 它的判断越"确定"
  各条柱子的含义:
  - "VIIRS only": 仅用VIIRS时, 后验离均匀先验多远 → VIIRS提供的信息量
  - "S2 only": 仅用S2时的信息量
  - "Joint(Fusion)": 两个传感器联合融合时的信息量
  关键对比: Joint的KLD应该大于任何一个单传感器——如果融合真的整合了互补信息的话。
  实际结果: Joint KLD ≈ VIIRS KLD ≈ S2 KLD → 融合没有增加信息! 因为两个传感器在互斥, 融合抵消了。

=== 右图说明 (信息抑制率空间分布) ===
每个像素的颜色 = (单传感器信息量 - 融合后信息量)/单传感器信息量
  - 蓝色(0%): 融合后信息量没有减少 → 两个传感器在这个像素上一致
  - 红色(100%): 融合后信息量完全消失 → 两个传感器在这个像素上完全矛盾
可以看到: 研究区域内大面积是红色/橙色 → 大部分像素上两个传感器都在互相抵消。

=== 关键解读 ===
信息抑制率的物理含义: "本来S2或VIIRS单独使用时各自有一定把握, 但当你把两者放在一起时,
 贝叶斯模型发现它们在互相矛盾, 于是'撤回'了确定性, 承认自己不确定。"
这不是融合的失败——这是融合正确识别了传感器之间的冲突, 并诚实地表达了不确定性。

=== 制作建议 ===
- 标题: "信息论透视: 融合没有增加信息, 因为传感器在互相否定"
- 左图KLD柱状图占45%, 右图抑制率空间分布占55%
- 左图下方标注: "Joint KLD 未超过 Single KLD → 传感器互斥"
- 右图下方标注: "大面积红=传感器互相矛盾, 融合'撤回'确定性"
""")

# ============================================================
# Page 08: D-S证据理论 vs 贝叶斯
# ============================================================
d = PPT / 'page_08'; d.mkdir()
shutil.copy(S2 / 'paper/figures/fig8_interval_comparison.png', d / '08a_interval_comparison.png')
shutil.copy(S2 / 'paper/figures/fig6_bayes_ds_v4.png', d / '08b_bayes_vs_ds.png')
with open(d/'说明.txt', 'w', encoding='utf-8') as f:
    f.write("""=== 这一页讲什么 ===
对比两种融合范式——Dempster-Shafer证据理论和贝叶斯层次模型——在传感器冲突时的表现差异。

=== 什么是D-S证据理论? ===
D-S给每种可能性分配一个"信度"mass:
  对传感器k: m_k({D})=p_k·(1-σ) 支持"损毁"; m_k({U})=(1-p_k)·(1-σ) 支持"未损毁"; m_k(Θ)=σ "不确定"
  D-S组合规则: 两个mass函数通过Dempster规则合并, 冲突会被归一化丢弃。
  输出: Bel(损毁)=信任下界, Pl(损毁)=信任上界, [Bel, Pl]区间 = 不确定性(epistemic uncertainty)

=== 左图说明 (区间对比) ===
每个点代表一个像素, 横轴=贝叶斯95%HDI宽度, 纵轴=D-S的[Bel, Pl]区间宽度。
如果两者一致, 点应该在对角线附近。
实际结果: D-S区间宽度远小于贝叶斯HDI宽度(点在对角线以下)。
→ D-S过于自信! 它把传感器冲突在归一化中丢弃了, 导致输出的不确定性区间很窄。
→ 贝叶斯把冲突转化为了宽后验(高不确定性), 更诚实。

=== 右图说明 (Bayes vs D-S 分类一致性) ===
每个像素按照"贝叶斯判断"和"D-S判断"的交叉分类着色:
  - 蓝色: 两者都认为"损毁"
  - 红色: 两者都认为"未损毁"
  - 绿色/黄色: 一个认为损毁, 另一个认为未损毁 → 分歧
可以看到: 右图中大量像素是绿色/黄色 → D-S和贝叶斯在大量像素上给出了相反的结论。
原因: 当传感器高度冲突时, D-S通过归一化丢弃了冲突, 导致Bel(D)→0(过于保守);
      而贝叶斯后验收敛至先验(均匀分布)→p≈0.5附近(诚实的不确定)。

=== 核心结论 ===
在传感器高度分歧的遥感场景中, D-S过于保守(Bel→0), 贝叶斯收敛至先验(p→0.5),
两者都表达了"不确定", 但贝叶斯的不确定性更诚实(宽区间), D-S的不确定性被冲突惩罚机制吞掉了。

=== 制作建议 ===
- 标题: "两种融合哲学: D-S丢弃冲突, 贝叶斯量化冲突"
- 左图(区间对比)占40%, 右图(Bayes vs D-S分类)占60%
- 底部一行总结: "D-S信度区间窄但不诚实 | 贝叶斯后验宽但承认不确定"
""")

# ============================================================
# Page 09: UNOSAT验证
# ============================================================
d = PPT / 'page_09'; d.mkdir()
shutil.copy(S2 / 'paper/figures/fig3_unosat_validation_v4.png', d / '09_unosat_validation.png')
with open(d/'说明.txt', 'w', encoding='utf-8') as f:
    f.write("""=== 这一页讲什么 ===
用UNOSAT(联合国卫星应用项目)的专家目视标注作为外部参考, 评估我们的方法到底准不准。

=== UNOSAT是什么? ===
联合国训练研究所(UNITAR)的UNOSAT项目, 由遥感专家在高分辨率卫星影像上
逐栋肉眼判读"这栋建筑是否被损毁"。
我们用的是马里乌波尔Livoberezhnyi区的标注(2022年5月12日)。
注意: UNOSAT不是绝对真值! 它主要捕捉可见的建筑结构损毁, 可能遗漏功能损毁和亚阈值变化。

=== 图片说明 (四面板验证图) ===
(a) 验证区域空间分布: 底图显示UNOSAT标注的建筑点位(红色=损毁, 蓝色=未损毁)。
    可以看到标注集中在一个子区域(市中心的Livoberezhnyi区)。
(b) 分数分布对比: 横轴=损毁概率, 纵轴=密度。蓝色曲线=UNOSAT标注为"未损毁"建筑的分数分布,
    红色曲线="已损毁"建筑的分数分布。理想情况下两组应有明显分离。
(c) ROC曲线: 横轴=假阳率, 纵轴=真阳率。AUC越大越好(>0.5)。
    图中比较了S2-only, VIIRS-only, 融合, D-S四条ROC曲线。
(d) PR曲线: 横轴=召回率, 纵轴=精确率。对不平衡数据更敏感的指标。

=== 验证结果 ===
- S2-only AUC: 相对较高(0.6-0.7范围)——S2对建筑结构变化有一定检测能力
- VIIRS-only AUC: 接近0.5——VIIRS在UNOSAT标注上几乎随机(因为UNOSAT标注的是建筑结构损毁, VIIRS测的是灯光)
- 融合 AUC: 在S2和VIIRS之间——融合没有提升, 因为两个传感器信息冲突
- D-S AUC: 同样没有提升

核心发现: 融合在UNOSAT验证上AUC没有提升 → "当两个传感器测的是不同物理量时, 融合不会自动变好,
  它只会诚实表达'我不知道'——这本身是比'AUC更高'更有价值的信息。"

=== 制作建议 ===
- 标题: "UNOSAT外部验证: 融合为什么没有提升AUC?"
- 四面板图放中间, 占80%
- 图下方放关键AUC数字(如"S2 AUC=0.67 | VIIRS AUC=0.52 | Fusion AUC=0.65")
- 底部一句话: "AUC没有提升 ≠ 融合没用。融合的价值在于告诉你'什么时候该不相信传感器的判断'"
""")

# ============================================================
# Page 10: 四传感器拓展 + 结论
# ============================================================
d = PPT / 'page_10'; d.mkdir()
shutil.copy(US / 'paper_figures/fig1_damage_maps.png', d / '10a_4sensor_maps.png')
shutil.copy(US / 'paper_figures/fig4_sensor_agreement.png', d / '10b_4sensor_corr.png')
with open(d/'说明.txt', 'w', encoding='utf-8') as f:
    f.write("""=== 这一页讲什么 ===
从两传感器拓展到四传感器, 展示"更多传感器→更完整覆盖→更诚实的融合"这一逻辑链,
并总结两套方案的互补价值。

=== 四传感器拓展 ===
在两传感器(S2+VIIRS)基础上, 我们额外加入了:
  Sentinel-1 SAR: 雷达卫星, 发射微波并接收回波。对建筑结构变化敏感(完整建筑vs废墟的雷达回波不同),
    且不受云层影响(微波穿透云层)。我们做了直方图匹配归一化来消除轨道差异。
  InSAR相干性: 比较两次雷达拍摄之间的相位一致性。地表变化(建筑坍塌、瓦砾堆位移)→相干性下降。
    我们做了时间基线归一化(τ=50天指数模型)来消除不同干涉对的时间差异。

=== 左图说明 (五面板损害图) ===
(a) VIIRS: 大部分黑色(几乎无损害信号) → 同两传感器方案
(b) Sentinel-2: 有空间结构的损害评分 → 同两传感器方案
(c) SAR: 灰色均匀(几乎无信号) → 雷达回波对建筑→废墟的转变不敏感(C波段散射特性相似)
(d) InSAR: 部分区域有信号 → 相干性下降的区域(可能对应损毁)
(e) 贝叶斯融合: 后验均值p≈0.176 — 比两传感器方案的0.155稍高, 因为四个传感器"拉"的方向不同

=== 右图说明 (传感器散点矩阵) ===
六个面板, 每个面板是两两传感器的散点图(横轴=传感器A的p, 纵轴=传感器B的p)。
每个面板标注了相关系数ρ。全部|ρ|<0.11 → 四传感器也是近乎正交的!

=== 两方案对比总结 ===
维度            | 两传感器方案(主线)      | 四传感器方案(拓展)
传感器数量       | 2 (S2+VIIRS)            | 4 (+SAR+InSAR)
融合方法         | MCMC完整贝叶斯           | 解析Beta-Binomial共轭
外部验证         | UNOSAT(有)              | 合成验证(无真实标签)
信息论分析       | KLD/JSD详细分析         | 基础KLD
SAR/InSAR       | 下载了但未使用           | 使用但预处理较简易
核心发现         | 传感器信号冲突,46.8%抑制 | 四传感器近乎正交,|ρ|<0.11
融合p            | ≈0.155                   | ≈0.176

=== 未来整合方向 ===
"两方案的结合点": 把四传感器数据放入两传感器方案的完整MCMC框架中,
用UNOSAT做外部验证, 做KLD/JSD信息增益分析, 做D-S vs Bayesian对比。
这将是"方法完整+传感器全面"的完整方案。

=== 制作建议 ===
- 上半部分: 五面板图 + 散点矩阵, 左右各50%
- 下半部分: 对比总结表(两方案8行×3列), 最后一行"未来方向"用红色高亮
- 底部大字(20pt): "两方案互补 → 四传感器+MCMC+UNOSAT = 完整方案"
""")

print("PPT结构(详细版)已创建:")
for d in sorted(PPT.glob('page_*')):
    n = d.name
    files = [f.name for f in d.glob('*') if f.suffix in ['.png','.jpg']]
    print(f"  {n}: {len(files)} images")
